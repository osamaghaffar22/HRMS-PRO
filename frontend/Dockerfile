FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json* ./
RUN npm install

# Copy source
COPY . .

# Build the Next.js app
RUN npm run build

# Start the application
CMD ["npm", "run", "start"]
